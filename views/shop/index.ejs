<%- include('../includes/head.ejs') %>
<body>
    <%- include('../includes/navigation.ejs') %>
    <main>
        <!-- <p>List of all the products...</p> -->
        <% if(prods.length > 0) { %>
        <div class="grid">
            <% prods.forEach((val) => { %>
            <article class="card product-item">
                <header class="card__header">
                    <h1 class="product__title"><%= val.title %></h1>
                </header>
                <div class="card__image">
                    <img src="<%= val.imageUrl %>" alt="<%= val.title %>">
                </div>
                <div class="card__content">
                    <h2 class="product__price">$<%= val.price %></h2>
                    <p class="product__description"><%= val.description %></p>
                </div>
                <div class="card__actions">
                    <%- include('../includes/add-to-cart.ejs', {product: val}) %>
                </div>
            </article>
            <% }); %>
        </div>
        <% } else { %>
            <center>
                <h1>No Product</h1>
            </center>
        <% } %>
    </main>
<%- include('../includes/end.ejs') %>
